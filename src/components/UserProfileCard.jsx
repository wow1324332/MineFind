// src/components/UserProfileCard.jsx
import React from 'react';

export default function UserProfileCard() {
  // 💡 화면에 아무것도 그리지 않고 투명하게 처리하여 컴포넌트를 잠재웁니다.
  // 다른 파일에서 이 컴포넌트를 호출해도 에러 없이 그냥 무시됩니다.
  return null;
}
